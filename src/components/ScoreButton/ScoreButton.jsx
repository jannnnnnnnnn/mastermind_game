import React from "react";
import anyword from "./ScoreButton.module.css";

const ScoreButton = (props) => (
  <button className={anyword.button} btn btn-default>
    ✔
  </button>
);

export default ScoreButton;
